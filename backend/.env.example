# ============================================
# AASILA ENVIRONMENT CONFIGURATION
# Copy this file to .env and fill in real values.
# NEVER commit .env — only this .env.example file.
# ============================================

# --- Application ---
ENVIRONMENT=development                # development | staging | production
ASILA_MASTER_KEY=                      # Admin key for tenant provisioning ops. Generate: openssl rand -hex 32
PLATFORM_API_KEY=                      # Platform access key. Generate: openssl rand -hex 32
SECRET_KEY=                            # Used to sign session JWTs (if applicable). Generate: openssl rand -hex 32
ALLOWED_ORIGINS=http://localhost:5173  # Comma-separated list of allowed CORS origins

# --- Platform Database (Postgres) ---
POSTGRES_USER=asila_admin
POSTGRES_PASSWORD=                     # Set a strong password, never reuse across environments
POSTGRES_DB=asila_platform
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# --- Redis (Task Queue / ARQ) ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=                        # Required in production, optional for local dev
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/0

# --- Google Gemini (Vision extraction + generation) ---
GOOGLE_API_KEY=                        # Get from https://aistudio.google.com/apikey
GEMINI_MODEL=gemini-2.5-flash-lite     # Or whichever model version is pinned
EMBEDDING_MODEL=models/gemini-embedding-001

# --- HashiCorp Vault (Optional) ---
# VAULT_URL=http://localhost:8200
# VAULT_TOKEN=
